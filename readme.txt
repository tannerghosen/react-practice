You must install react-router-dom, it is not included by default with visual studio create react projects!
For notes on how to use the router (because it is confusing), look at the notes in App.js and Layout.js
For notes on how to use React, look at the code and you'll get a general gist
Remember, anything in a return(<> </>); is normally used to render HTML tags, and 
functions/variables are called through {}'s.